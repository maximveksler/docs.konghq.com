{
  "fields": [
    {
      "service": {
        "eq": null,
        "type": "foreign",
        "reference": "services"
      }
    },
    {
      "consumer": {
        "eq": null,
        "type": "foreign",
        "reference": "consumers"
      }
    },
    {
      "config": {
        "fields": [
          {
            "api_address": {
              "type": "string",
              "required": true,
              "default": null,
              "examples": ["http://cleafy.com"],
              "description": "Specifies the URL pointing to the Cleafy API."
            }
          },
          {
            "accept_encoding": {
              "type": "string",
              "default": null,
              "description": "Specifies the type of encoding accepted from the backend server. This plugin does not support gzip-encoded requests."
            }
          }
        ],
        "type": "record",
        "required": true
      }
    }
  ]
}
