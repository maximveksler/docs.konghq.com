{
  "fields": [
    {
      "consumer": {
        "eq": null,
        "type": "foreign",
        "reference": "consumers"
      }
    },
    {
      "protocols": {
        "elements": {
          "one_of": [
            "http",
            "https"
          ],
          "type": "string"
        },
        "required": true,
        "type": "set",
        "default": [
          "http",
          "https"
        ]
      }
    },
    {
      "config": {
        "fields": [
          {
            "request_transformer": {
              "type": "string",
              "required": true,
              "description": "The .lua script to be used for the transformation.\nAvailable OpenResty variables and utils: [Check README](https://github.com/qnap-dev/kong-plugin-api-transformer#for-developer){:target=\"_blank\"}{:rel=\"noopener noreferrer\"}"
            }
          },
          {
            "response_transformer": {
              "type": "string",
              "required": true,
              "description": "The .lua script to be used for the transformation.\nAvailable OpenResty variables and utils: [Check README](https://github.com/qnap-dev/kong-plugin-api-transformer#for-developer){:target=\"_blank\"}{:rel=\"noopener noreferrer\"}"
            }
          },
          {
            "http_200_always": {
              "type": "boolean",
              "default": true,
              "description": "We may need to use HTTP 200 approach in API error handling in some business cases."
            }
          }
        ],
        "type": "record",
        "required": true
      }
    }
  ]
}
