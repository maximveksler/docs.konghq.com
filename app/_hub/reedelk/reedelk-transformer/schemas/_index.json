{
  "fields": [
    {
      "consumer": {
        "eq": null,
        "type": "foreign",
        "reference": "consumers"
      }
    },
    {
      "protocols": {
        "elements": {
          "one_of": [
            "http",
            "https"
          ],
          "type": "string"
        },
        "required": true,
        "type": "set",
        "default": [
          "http",
          "https"
        ]
      }
    },
    {
      "config": {
        "fields": [
          {
            "upstream_transformer_url": {
              "type": "string",
              "description": "The URL of the Reedelk REST flow endpoint to invoke for the upstream\nrequest transformation."
            }
          },
          {
            "downstream_transformer_url": {
              "type": "string",
              "description": "The URL of the Reedelk REST flow endpoint to invoke for the downstream\nrequest transformation."
            }
          }
        ],
        "type": "record",
        "required": true
      }
    }
  ]
}
